# ============================================================================
# KORTIX SUPER WORKER DOCKER COMPOSE - LOCAL DEVELOPMENT
# ============================================================================

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
      - ./backend/services/docker/redis.conf:/usr/local/etc/redis/redis.conf:ro
    command: redis-server /usr/local/etc/redis/redis.conf --save 60 1 --loglevel warning
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  backend:
    image: ghcr.io/suna-ai/suna-backend:latest
    platform: linux/amd64
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    volumes:
      - ./backend/.env:/app/.env:ro
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=
      - REDIS_SSL=False
    depends_on:
      redis:
        condition: service_healthy
      worker:
        condition: service_started

  worker:
    image: ghcr.io/suna-ai/suna-backend:latest
    platform: linux/amd64
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: uv run dramatiq --skip-logging --processes 4 --threads 4 run_agent_background
    volumes:
      - ./backend/.env:/app/.env:ro
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=
      - REDIS_SSL=False
    depends_on:
      redis:
        condition: service_healthy

  frontend:
    init: true
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_BACKEND_URL: ${NEXT_PUBLIC_BACKEND_URL:-https://api.kortix.darwisyah.com/v1}
        NEXT_PUBLIC_URL: ${NEXT_PUBLIC_URL:-https://kortix.darwisyah.com}
        NEXT_PUBLIC_ENV_MODE: ${NEXT_PUBLIC_ENV_MODE:-PRODUCTION}

    ports:
      - "3000:3000"
    depends_on:
      - backend

    # -------------------------------------------------------------------------
    # Environment variables for auth and URL handling
    # -------------------------------------------------------------------------
    environment:
      - NEXTAUTH_URL=https://kortix.darwisyah.com
      - NEXT_PUBLIC_APP_URL=https://kortix.darwisyah.com
      - NEXT_PUBLIC_URL=https://kortix.darwisyah.com
      - HOST=0.0.0.0
      - PORT=3000

    # -------------------------------------------------------------------------
    # STARTUP PATCHES:
    # 1. Fix server.js: Prevent Next.js from using HOSTNAME=0.0.0.0 for URLs
    # 2. Fix auth callback: Force correct origin instead of nextUrl.origin
    #
    # These patches run at container startup and survive redeploys.
    # -------------------------------------------------------------------------
    command:
      - sh
      - -lc
      - |
        echo "🔧 Applying startup patches..."

        # Patch 1: Fix server.js hostname binding
        node -e "
        const fs = require('fs');
        const serverPath = '/app/server.js';
        let content = fs.readFileSync(serverPath, 'utf8');
        const original = content;
        content = content.replace(
          \"const hostname = process.env.HOSTNAME || '0.0.0.0'\",
          \"const hostname = process.env.HOST || '0.0.0.0'\"
        );
        if (content !== original) {
          fs.writeFileSync(serverPath, content);
          console.log('✅ Patched server.js: HOSTNAME -> HOST');
        } else {
          console.log('ℹ️  server.js already patched or pattern not found');
        }
        "

        # Patch 2: Fix auth callback to use hardcoded origin
        node -e "
        const fs = require('fs');
        const callbackPath = '/app/.next/server/app/auth/callback/route.js';
        if (fs.existsSync(callbackPath)) {
          let content = fs.readFileSync(callbackPath, 'utf8');
          const original = content;
          // Replace: a.nextUrl.origin || \"https://kortix.darwisyah.com\"
          // With: \"https://kortix.darwisyah.com\"
          content = content.replace(
            /a\.nextUrl\.origin\s*\|\|\s*['\"]https:\/\/kortix\.darwisyah\.com['\"]/g,
            '\"https://kortix.darwisyah.com\"'
          );
          if (content !== original) {
            fs.writeFileSync(callbackPath, content);
            console.log('✅ Patched auth callback: forced correct origin');
          } else {
            console.log('ℹ️  auth callback already patched or pattern not found');
          }
        } else {
          console.log('⚠️  auth callback file not found at expected path');
        }
        "

        echo "🚀 Starting Next.js server..."
        exec node /app/server.js

volumes:
  redis_data:
